[project]
name = "day12"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Subhradeep Sikder", email = "isdsikder@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "groq>=1.6.0",
    "numpy>=2.5.2",
    "python-dotenv>=1.2.2",
    "sentence-transformers>=5.7.0",
]

[project.scripts]
day12 = "day12:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
